server:
  port: 8004

spring:
  application:
    name: orderProvider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://localhost:3306/spring?useSSL=false&serverTimezone=Asia/Shanghai"
    username: root
    password: 123456